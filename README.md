# Learning Centers — Vue 3 + DRF + Postgres + Docker

## Быстрый старт
```bash
cp .env.example .env
docker compose up -d --build
# UI: http://localhost:5173
# API docs: http://localhost:8000/api/docs/
```
